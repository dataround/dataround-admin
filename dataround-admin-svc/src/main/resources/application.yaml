server:
  port: 5680
  servlet:
    context-path: /dataadmin

spring:
  application:
    name: dataround-admin

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://10.8.0.1:5432/dataround_admin?currentSchema=public
    username: postgres
    password: "dataround.io"
  sql:
    init:
      data-locations: classpath:postgresql-schema.sql
      # mode: always - execute script on every startup
      mode: always
      encoding: UTF-8

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml

dataround:
  login:
    interceptor:
      escape: "/api/login, /api/logout, /api/captcha, /error, /static/**, /**/*.css, /**/*.js, /**/*.html, /**/*.png, /**/*.jpg, /**/*.gif, /**/*.svg, /**/*.woff, /**/*.woff2, /**/*.ttf, /**/*.ico"

# Logger Config
logging:
  level:
    io.dataround: debug
